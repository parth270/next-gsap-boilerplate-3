import React from 'react';
import classes from './Logo.module.css';

const Logo = () => {

    return(
        <p className={classes.logo}>
            LOGO.
        </p>
    );
}

export default Logo;